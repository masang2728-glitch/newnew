# 조직 계층 구조 확장 계획 (plan-org-hierarchy.md)

작성일: 2026-08-28
대상: `vacation-overtime-pwa`를 더 큰 조직(장비단 → 공장 → 직장)에서 운영할 수 있도록 확장

> 참고: 이 문서는 앱을 처음 만들 때 작성한 `plan.md`(기술 스택/마이그레이션 계획)와는 별개입니다. 그 문서는 그대로 두고, 이 문서는 조직 계층 구조 기능만 다룹니다.

---

## 1. 현재 구조와 한계

### 현재 조직 모델
지금 앱의 `team`(팀명)은 딱 **한 단계**만 존재합니다. 사용자가 로그인 시 자유롭게 입력한 문자열이 곧 팀이고, 같은 문자열을 입력한 사람끼리 같은 캘린더/신청 내역을 공유합니다.

```
team = "전차해체" ──▶ 이 이름으로 로그인한 사람들만 서로의 신청을 봄
```

### 실제 조직 구조 (사용자 설명 기준)

```
장비단
 ├─ 전차공장
 │   ├─ 유압직장
 │   ├─ 전차차체직장
 │   └─ 전차해체직장
 ├─ 자주포공장
 │   └─ (직장들...)
 └─ 차량공장
     └─ (직장들...)
```

지금 앱의 `team`은 이 구조에서 **가장 하위 단위인 "직장"**에 해당합니다. 직장 단위 운영(구성원 휴가/야근 확인)은 지금도 잘 동작합니다.

### 빠진 부분
"직장"보다 상위 관리자(예: 전차공장 전체를 관리하는 사람)는 자기 소속 직장들(유압/전차차체/전차해체...)의 신청 내역을 **합쳐서** 보고 싶은데, 지금은:
- 직장마다 따로 로그인해서 각각 확인해야 함
- 직장 간 데이터를 묶어서 보여주는 화면이 없음
- "이 직장이 어느 공장 소속인지"를 앱이 전혀 모름 (그냥 문자열일 뿐)

---

## 2. 설계 방향

### 핵심 원칙: 기존 구조는 그대로 두고, "소속 매핑"만 추가한다

직장 단위의 로그인·신청·확인 플로우(지금 잘 동작하는 부분)는 **하나도 바꾸지 않습니다.** 대신:

1. "이 직장은 어느 공장 소속이다"라는 매핑 정보를 새 테이블에 저장
2. 공장 단위로 로그인하는 새로운 관리자 계층 추가
3. 공장 관리자는 소속 직장들의 신청 내역을 자동으로 합쳐서 보는 대시보드 화면 사용

이렇게 하면 기존 데이터·기존 사용자 경험을 전혀 건드리지 않고 위에 한 층만 얹는 구조가 됩니다. (참고: 이전에 만들었다가 없앤 "여러 팀 직접 선택하는 대시보드"와 비슷해 보이지만, 그때는 사람이 매번 팀을 수동으로 골라야 했다면 이번엔 조직도에 따라 자동으로 묶인다는 게 차이입니다.)

### 데이터 모델

새 테이블 `org_groups` 하나만 추가합니다.

```sql
create table org_groups (
  team text primary key,   -- 기존 team 값과 정확히 일치 (예: "전차해체")
  factory text not null,   -- 소속 공장명 (예: "전차공장")
  corps text                -- 소속 장비단명 (선택, 필요할 때만 사용)
);
```

- 기존 `vacation_requests` / `overtime_requests` / `team_members` 테이블은 **전혀 수정하지 않습니다.** (team 컬럼 그대로 사용)
- 공장 단위 집계는 "이 공장 소속 team 목록을 org_groups에서 조회 → 그 team들의 신청을 모아서 보여주기" 방식으로 동작합니다.
- 장비단 단위까지 필요해지면 같은 테이블의 `corps` 컬럼을 그대로 활용해 한 단계 더 확장할 수 있습니다 (아래 "확장성" 참고).

### 화면/권한 구조

| 계층 | 로그인 방식 | 보이는 것 |
|---|---|---|
| 직장 구성원 (기존) | 이름 + 순번 + 팀명(직장명) | 자기 직장의 캘린더/신청 (기존 그대로) |
| 직장 관리자 (기존) | 위 + 관리자 암호 | 자기 직장의 신청 확인/관리 (기존 그대로) |
| **공장 관리자 (신규)** | 공장명 + 공장관리자 암호 | 소속 직장 전체를 합친 현황 (신규) |
| 장비단 관리자 (신규, 필요시) | 장비단명 + 암호 | 소속 공장 전체(직장까지)를 합친 현황 (신규) |
| 최고관리자 (기존) | 최고관리자 암호 | 등록된 모든 직장 목록/삭제 (기존 그대로, 이번 변경과 무관) |

공장 관리자 로그인은 기존 "관리자이신가요?" 방식과 동일한 패턴(공유 암호 + 본인이 소속 이름을 직접 입력)으로 만들어서, 지금 앱의 보안 수준(단순함 우선, 실사용 편의 우선)과 일관되게 갑니다.

### 공장 대시보드 화면 (신규)

공장 관리자가 로그인하면:
- 소속 직장 목록과 각 직장의 이번 달 휴가/야근 건수를 한눈에 보여주는 요약
- 직장별로 구분된 섹션 아래에 신청 내역을 나열 (완전히 뒤섞지 않고, "어느 직장 사람인지"는 항상 표시)
- 필요하면 예전에 만들었던 "구분/총원/사고/현재원 + 유형별" 표 형태(사용자가 보여준 사고현황표 양식)를 여기서 재사용 가능 — 이번엔 수동 선택이 아니라 자동으로 그 공장 소속 직장들이 채워짐

---

## 3. 단계별 구현 계획

### Phase 1 — 데이터 모델 + 소속 등록 화면
- `org_groups` 테이블 생성 (SQL 마이그레이션 파일 추가)
- 최고관리자 화면(또는 신규 "조직 관리" 화면)에 "이 직장 → 이 공장" 매핑을 입력/수정하는 간단한 폼 추가
- 기존에 이미 만들어진 직장들은 최고관리자가 한 번씩 수동으로 공장을 지정해야 함 (아래 "마이그레이션" 참고)

### Phase 2 — 공장 관리자 로그인 + 대시보드
- 입장 화면에 "공장 관리자이신가요?" 진입점 추가 (공장명 + 암호 입력)
- 공장 대시보드 화면: 소속 직장 목록 조회 → 직장별 신청 내역을 실시간으로 합쳐서 표시
- 요약 통계(직장별 건수, 전체 합계) 표시

### Phase 3 — 장비단 레벨 (필요 시점에 진행)
- 지금 당장 필요하지 않다면 보류. 필요해지면 `org_groups.corps` 컬럼을 이용해 Phase 2와 동일한 패턴으로 한 단계 더 추가

### Phase 4 — 안내 문구 정리
- 직장 구성원 입장 화면의 안내 문구("같은 팀원끼리는 팀명을 정확히 동일하게...")에 소속 공장 관련 혼동이 없도록 필요시 문구 보완

---

## 4. 마이그레이션 (기존 데이터는 어떻게 되나)

- **기존 직장/구성원/신청 데이터는 전혀 손대지 않습니다.** `org_groups`는 완전히 별도의 새 테이블이라, 채워 넣기 전까지는 아무 영향이 없습니다.
- 다만 `org_groups`에 매핑이 안 된 직장은 공장 대시보드에 나타나지 않습니다 — 그래서 Phase 1에서 기존 직장들을 한 번씩 공장에 배정하는 작업이 필요합니다 (직장 수가 많지 않다면 최고관리자가 목록 보면서 하나씩 지정하면 됨).
- 매핑을 깜빡한 직장이 있어도 기존 방식(직장 단위 로그인)은 그대로 동작하니, 서비스 중단 없이 순차적으로 등록할 수 있습니다.

---

## 5. 고려했던 다른 방식과 선택 이유

| 방식 | 설명 | 선택 여부 |
|---|---|---|
| **매핑 테이블 추가 (채택)** | `org_groups` 별도 테이블로 직장→공장 연결 | ✅ 기존 데이터/로그인 흐름 무변경, 몇 단계든 확장 쉬움 |
| team_members/requests에 factory 컬럼 직접 추가 | 매번 신청/등록 시 공장 정보도 같이 저장 | ❌ 모든 기존 행 마이그레이션 필요, 매 신청마다 데이터 중복 저장 |
| 로그인 시 직장 구성원도 공장명을 직접 입력 | 입장 화면에 필드 하나 추가 | ❌ 모든 일반 구성원의 매 로그인마다 입력 부담 증가, 오타로 인한 매칭 실패 위험 |

---

## 6. 남은 결정사항 (진행 전 확인 필요)

1. **장비단 레벨이 지금 당장 필요한가요, 아니면 공장 레벨만 우선 구현할까요?**
2. **공장 관리자 암호는 공장마다 다르게 할까요, 아니면 지금처럼 공유 암호 하나로 할까요?**
3. **지금 등록된 직장이 몇 개나 되나요?** (마이그레이션 작업량 가늠용)
