# 휴가/야근 신청 공유 캘린더 — 기능 명세서 (spec.md)

기존 React Native(Expo) 버전에서 확정된 요구사항을 그대로 승계하여 PWA로 재구현하기 위한 명세서.

## 1. 개요

사내 인원들이 휴가 또는 야근을 신청하면, 팀 전체가 함께 보는 공유 캘린더에 자동으로 반영되는 웹 앱(PWA). 별도 로그인 없이 **이름 + 팀명**만 입력해서 입장하며, 세션이 유지되는 동안 그 이름/팀으로 활동한다.

## 2. 입장(로그인) 방식

- 최초 접속 시 이름/팀명 입력 화면을 보여준다.
- 이름, 팀명을 입력하고 "입장" 버튼을 누르면 해당 이름/팀이 세션 동안 유지된다.
- 새로고침·재접속해도 동일 브라우저에서는 세션이 유지된다 (localStorage 기반).
- 화면 상단에 "{팀명} · {이름}님으로 접속 중" 형태로 항상 표시한다.
- 이름 또는 팀명이 비어 있으면 입장을 막고 안내 문구를 띄운다.
- 같은 팀원끼리는 팀명을 정확히 동일하게 입력해야 같은 캘린더를 공유한다는 안내 문구를 표시한다.
- **관리자 모드**: "관리자이신가요?" 링크를 누르면 PIN 입력란이 나타난다. 사전에 정해진 관리자 PIN을 입력하면 관리자 권한으로 입장한다. PIN이 틀리면 입장을 막고 오류를 표시한다. PIN을 비워두면 일반 사용자로 입장한다.
- 메인 화면에 "다른 이름/팀으로 전환" 링크를 두어 세션을 초기화하고 다시 입장할 수 있게 한다.

## 3. 팀 구성과 격리

- 팀은 사전 등록 없이 입장 시 자유 입력으로 결정된다 (팀명 = 사실상의 공유 코드).
- **팀 간 완전 격리**: 데이터 저장/조회 경로 자체가 팀별로 분리되어, 다른 팀의 데이터를 조회할 방법이 없다.
- 관리자는 PIN으로 식별되는 전역 권한이며, 자신이 입장한 팀 범위 내에서 관리자 권한(타인 신청 삭제)을 갖는다. 다른 팀을 관리하려면 그 팀명으로 다시 입장하면 된다.

## 4. 메인 화면

- 상단에 접속 정보 (팀/이름/관리자 여부) 표시.
- 큼직한 배너 2개만 배치: **휴가 신청**, **야근 신청**.
- 각 배너를 클릭하면 해당 유형 전용 화면으로 이동하며, 그 화면 안에 "신청하기"와 "캘린더 보기" 탭이 함께 들어있다.
- **휴가 화면과 야근 화면은 완전히 분리**되어 있어, 서로의 신청 내역이 전혀 보이지 않는다. 통합 캘린더는 만들지 않는다.
- 휴가 화면은 파란색 계열, 야근 화면은 주황색 계열 테마로 구분한다.
- 각 유형 화면에는 "메인으로 돌아가기" 버튼을 둔다.

## 5. 휴가 신청 플로우

### 5.1 날짜 선택
- 월간 달력에서 하루 또는 여러 날짜를 다중 선택할 수 있다.
- 지난 날짜는 선택할 수 없다 (자동 비활성화).

### 5.2 휴가 유형 (택 1, 필수)
- **연가**: 종일 또는 임의 시간대 (수동 시간 선택 가능).
- **오전반차**: 선택 즉시 시작 08:00 / 종료 12:00 로 자동 고정.
- **오후반차**: 선택 즉시 시작 12:00 / 종료 17:00 로 자동 고정.
- **공가**: 사유 입력란 필수 노출.
- **청원**: 사유 입력란 필수 노출.

### 5.3 시작시간 / 종료시간
- 연가·공가·청원 선택 시: 07:00~17:00, 1시간 단위로 클릭해서 선택하는 슬롯 UI (텍스트 직접 입력 아님). 시작/종료 각각 단일 선택, 선택 안 하면 종일로 간주.
- 오전반차/오후반차 선택 시: 자동 고정값을 읽기 전용으로 표시 (수동 조작 불가).
- 종료시간은 시작시간보다 늦어야 한다 (검증).

### 5.4 행선지
- 자유 입력 텍스트란 (예: "부산 출장", "자택"). 모든 휴가 유형에 공통으로 노출.

### 5.5 사유
- 공가/청원 선택 시에만 노출되는 자유 입력 텍스트란. 미입력 시 제출 차단.

### 5.6 제출
- "신청 확인" 클릭 시 선택한 모든 날짜에 대해 각각 1건씩 신청이 생성된다 (이름/날짜/유형/시간/행선지/사유).
- 이미 본인이 신청한 날짜가 포함되어 있으면 제출을 막고 안내한다.
- 제출 성공/실패를 토스트로 명확히 알린다.

## 6. 야근 신청 플로우

### 6.1 날짜 선택 및 유형
- 달력에서 날짜를 클릭하면 즉시 "조출 / 야근 / 조출+야근 둘 다" 중 하나를 고르는 선택 모달이 뜬다.
- 같은 날짜를 다시 클릭하면 모달이 다시 열리며, 선택 해제도 가능하다.
- 여러 날짜를 각각 다른 조합(조출/야근/둘다)으로 선택할 수 있다.

### 6.2 제출
- "둘 다" 선택한 날짜는 조출 1건 + 야근 1건, 총 2건의 신청으로 저장된다.
- 이미 본인이 신청한 날짜가 포함되어 있으면 제출을 막는다.
- 성공/실패 토스트 표시.

## 7. 신청 취소

- **본인만 취소 가능**: 자신이 등록한 신청 건만 "취소" 버튼이 동작한다.
- **관리자는 예외적으로 타인의 신청도 취소 가능** (자신이 입장한 팀 범위 내에서).
- 취소 전 확인 대화상자를 띄운다.

## 8. 캘린더 보기 (휴가용/야근용 각각 독립)

- 월간 달력에 날짜별로 그날 신청한 인원 수를 **숫자 배지**로 표시한다 (단순 점 표시 아님).
- 상단에 월간 요약 바: "{연도}년 {월}월 신청 현황 · 총 N건 · N명".
- 이전 달/다음 달 이동 가능, 이동 시 요약도 해당 월 기준으로 갱신된다.
- 특정 날짜를 클릭하면 그날 신청한 사람들의 이름과 상세(유형/시간/행선지 등)를 목록으로 볼 수 있다.
- **(휴가 전용) 월별 명단**: 캘린더 보기 하단에 이번 달 전체 휴가 신청 내역을 이름 / 휴가유형 / 시간 / 행선지 컬럼으로 목록 표시한다 (날짜순 정렬).
- 관리자는 "신청하기" 탭의 "나의 신청 내역" 대신 "전체 신청 내역 관리"로 표시되어 팀 전체 신청 건을 열람/취소할 수 있다.

## 9. 데이터 모델

공통 신청 레코드:

```
id: string (고유값)
name: string        // 신청자 이름
date: string         // "YYYY-MM-DD"
createdAt: number    // 신청 시각(epoch ms)

// 휴가 신청 전용
leaveType?: "연가" | "오전반차" | "오후반차" | "공가" | "청원"
startTime?: string   // "HH:MM"
endTime?: string     // "HH:MM"
destination?: string // 행선지
reason?: string       // 사유 (공가/청원)

// 야근 신청 전용
subType?: "조출" | "야근"
```

- 휴가와 야근은 **서로 다른 저장 공간(테이블/경로)**에 완전히 분리 저장한다.
- 팀별로도 저장 공간이 분리된다 (팀 A의 데이터를 팀 B 화면에서 조회할 방법 자체가 없어야 한다).
- 같은 팀 내 모든 사용자는 실시간 또는 준실시간으로 동일한 데이터를 공유해서 본다.

## 10. 예외 처리

- 이름/팀명 미입력 시 입장 차단 및 안내.
- 관리자 PIN 오입력 시 입장 차단 및 안내.
- 지난 날짜 신청 차단.
- 본인 중복 신청(같은 날짜) 차단.
- 본인이 등록하지 않은 신청 건은 취소 불가 (관리자 제외).
- 공가/청원 신청 시 사유 미입력이면 제출 차단.
- 종료시간이 시작시간보다 빠르면 제출 차단.

## 10-1. 앱 정보 / 요청사항 게시판

- 메인 화면 최하단에 "앱 정보" 링크가 있다.
- 클릭 시 이동하는 화면에서: 앱 생성일, 제작자(윤상경)를 보여준다.
- 그 아래 "요청사항 게시판"이 있어, 누구나 이름/팀과 함께 자유 텍스트로 요청사항(기능 요청, 버그 제보 등)을 등록할 수 있다.
- 게시판은 전체 팀 공용(단일 글로벌 목록)이며, 등록된 글은 실시간으로 모든 사용자에게 갱신되어 보인다.

## 11. UI/UX

- 모바일 우선 반응형 (PWA로 홈 화면 추가 설치 지원).
- 휴가 화면: 파란색 계열 테마 / 야근 화면: 주황색 계열 테마.
- 신청/취소/오류 상황에 토스트 알림으로 명확히 피드백.
- 큰 배너, 큰 터치 영역으로 모바일에서 조작하기 쉽게 구성.

## 11-1. 최고관리자 (앱 전체 관리자)

- 팀별 관리자(§7)와는 별개의 암호로 식별되는 전역 권한. 특정 팀에 소속되지 않고 앱 전체를 조회한다.
- 이름 입장 화면 하단의 "최고관리자이신가요?" 링크로 별도 암호를 입력해 전용 대시보드(`/super-admin`)로 입장한다.
- 대시보드에서 확인 가능한 정보:
  - 전체 팀 수, 전체 이용 인원 수, 전체 휴가/야근 신청 건수 (요약 카드)
  - 팀별 목록: 팀명, 해당 팀 이용 인원, 휴가/야근 신청 건수, 최근 활동일
- 팀별로 "팀 삭제" 기능 제공: 확인 후 해당 팀의 휴가/야근 신청 데이터를 전부 영구 삭제한다.
- 데이터 조회/삭제는 팀 스코프를 넘어서므로, 이 권한은 팀 관리자보다 상위 개념이다.

## 12. 비고 (기존 RN 버전과의 차이)

- 기존 React Native(Expo) + Firebase 버전은 그대로 유지하고, 본 spec은 **PWA(React 웹) + Supabase + Netlify** 스택으로 별도 재구현하기 위한 것이다.
- 이 앱은 이름/팀명 기반의 무인증 구조이므로, 데이터 접근 제어는 애플리케이션 로직/테이블 분리 수준이며 암호학적으로 강제되지 않는다는 한계를 그대로 승계한다.
